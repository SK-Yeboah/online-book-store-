# Build output — never copy into Docker build context
target/

# Secrets — must never reach the image
.env
*.env

# IDE and OS noise
.idea/
.vscode/
*.iml
.DS_Store
Thumbs.db

# Git history — not needed inside the image
.git/
.github/

# Test and documentation
src/test/
*.md
